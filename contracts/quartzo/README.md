# Quartzo Contract Fixtures

These fixtures are language-neutral conformance vectors for Quartzo clients.

Flutter tests consume this tree now. The future Obsidian Companion TypeScript tests must consume the same fixture semantics instead of deriving behavior from Flutter UI code.

`contract_manifest.json` contains only version metadata. It is not a model source of truth.
